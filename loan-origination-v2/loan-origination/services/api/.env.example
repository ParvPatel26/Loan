DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/loan_origination
JWT_SECRET=change-me-in-real-env
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
# Shared secret for server-to-server callers (the chat-agent backend reading
# the catalog / submitting completed applications) — must match agent-backend's
# CATALOG_API_KEY exactly.
SERVICE_API_KEY=change-me-in-real-env
